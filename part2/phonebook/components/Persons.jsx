import Name from "./Name";

// Component
const Persons = ({list, filter}) => {

/*      const filtered = list.filter((item) => item.name.toLowerCase().includes(filter.toLowerCase()));
 */
  return (
    <div>
{/*       {filtered.map(item => (
      < Name key={item.id} name={item.name} number={item.number} />
    ))} */}
    </div>
  );
};

 export default Persons;